// This file exists to provide the explicit /team/[teamId]/dashboard route.
// It re-uses the existing team dashboard client component defined at
// src/app/team/[teamId]/page.tsx so we don't duplicate logic.

export { default } from '../page';
